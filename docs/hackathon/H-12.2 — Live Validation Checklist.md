# H-12.2 — Live Validation Checklist

**Sprint:** H-12.2 · Live Data Validation Preparation
**Estado:** infraestructura preparada · **sin bloqueos** · a la espera de las claves
**Fecha:** 2026-08-06 · *actualizado en H-12.3 con la decisión de producto*

> **Alcance oficial de la hackathon (H-12.3).** El recorrido de la demo termina
> en la generación del mensaje, sobre los tres canales existentes: **Email,
> LinkedIn e Instagram**. **WhatsApp queda fuera del alcance** y no es requisito
> de validación. Su trabajo futuro vive en `docs/backlog/B-01 — WhatsApp Channel.md`.

Este documento es el guion exacto que ejecutaré cuando existan las claves. No
contiene resultados: los resultados van a `H-13 — Live Data Findings.md`.

---

## 1. Verificación de la cadena

Recorrida eslabón por eslabón. **No se modificó nada de lo que ya funcionaba.**

| # | Eslabón | Estado | Comprobación |
|---|---------|--------|--------------|
| 1 | `.env` | ✅ | Creado, en `.gitignore`, valores vacíos a la espera |
| 2 | Carga de `dotenv` | ✅ | Reparada en H-12 (`server.ts`); verificada leyendo un valor de prueba |
| 3 | `shared/config/env.ts` | ✅ | Única frontera con `process.env`; cadena vacía se trata como ausente |
| 4 | Composition Root | ✅ | `createGooglePlacesDiscovery(getGooglePlacesApiKey() ?? "")` — la clave se inyecta **en el arranque** |
| 5 | Places Adapter | ✅ | `places.googleapis.com/v1/places:searchText`, `FieldMask` con 7 campos |
| 6 | Lead Pipeline | ✅ | Descubrimiento → deduplicación → registro → análisis → Score |
| 7 | Opportunity View | ✅ | Renderiza; validada con datos en sprints anteriores |
| 8 | Pitch Generator | ✅ | Renderiza; **tres canales activos — fin del recorrido oficial** |

### Nota sobre el reinicio

El Composition Root resuelve la credencial **una sola vez, al arrancar**
(ADR-17 §6.3 P-4). Pegar la clave en `.env` con el servidor levantado no surte
efecto: **hay que reiniciarlo.**

---

## 2. Instrumentación disponible

Verificada, no ampliada. Lo que el reporte de ejecución imprime hoy:

| Señal pedida | Disponible | Origen |
|--------------|-----------|--------|
| Tiempo de Places | ✅ | `googlePlaces.ms` |
| Tiempo de IA | ✅ | `gemini.ms` + `attempts` + `failure` |
| **Tiempo de Score** | ⚠️ **no aislado** | Va dentro de `leadAnalyzer.ms`, junto con el análisis |
| Leads encontrados | ✅ | `googlePlaces.found` |
| Leads descartados | ✅ | `deduplication.before/after` |
| Motivo del descarte | ✅ | `Discarded: N (duplicados · ya en pantalla · sin nombre)` — H-12.1 |
| Duración total | ✅ | `Total Time:` |

**El único hueco es el tiempo de scoring.** `calculateOpportunityScore` se
ejecuta dentro de `analyzeProspects` y su coste queda sumado al del análisis con
Gemini, que lo domina por dos órdenes de magnitud. Consecuencia práctica: si el
Score resultara lento, el reporte **no permitiría distinguirlo** de una llamada
lenta al modelo.

No lo instrumento porque este sprint prohíbe añadir logs. Lo dejo señalado: si
durante la validación los tiempos de `leadAnalyzer` resultan sospechosos, será lo
primero que haya que separar.

Los logs **ya no se imprimen en producción** (H-12.1), verificado con
`NODE_ENV=production`.

---

## 3. Comprobaciones previas al arranque

```
□ .env contiene GOOGLE_PLACES_API_KEY con valor real
□ .env contiene GEMINI_API_KEY con valor real
□ La clave de Places tiene habilitada «Places API (New)» en Google Cloud
□ La clave tiene facturación activa en el proyecto
□ Servidor REINICIADO después de pegar las claves
□ Arranque sin errores en consola
```

---

## 4. Recorrido de validación

### 4.1 Recorrido oficial de la demo (H-12.3)

Siete pasos. **No se añaden pasos adicionales.** Termina en la generación del
mensaje sobre los tres canales existentes.

```
1 · BUSCAR LEADS
    □ Places responde — sin «Falta la clave»
    □ Aparecen Leads reales en pantalla
    □ La cabecera de descubrimiento muestra cifras coherentes con las tarjetas
    □ LeadCard se renderiza sin romperse
    □ Opportunity Score presente y dentro de rango

2 · VALIDAR OPPORTUNITY VIEW
    □ Abre al pulsar sobre un negocio
    □ El desglose del Score cuadra con el total
    □ Las ausencias se declaran, no desaparecen (R-38)

3 · VALIDAR AI SHOWCASE
    □ Muestra el rastro real del análisis
    □ Declara cuándo el contenido vino del respaldo

4 · GENERAR MENSAJE
    □ Gemini responde — el mensaje NO viene del respaldo
    □ El mensaje va firmado con el perfil real del usuario
    □ No afirma nada fuera de la lista de problemas (integridad H-09)
    □ Copiar funciona

5 · VALIDAR EMAIL
    □ Genera asunto además de cuerpo
    □ Registro adecuado al canal

6 · VALIDAR LINKEDIN
    □ Sin asunto (no aplica al canal)
    □ Longitud apropiada para nota de conexión

7 · VALIDAR INSTAGRAM
    □ Registro de mensaje directo
    □ Longitud apropiada para DM

FIN DEL RECORRIDO OFICIAL
```

**Comprobaciones complementarias** (fuera del recorrido oficial, útiles si hay
tiempo): la tarjeta «Siguiente oportunidad» tras copiar, que Biblioteca refleje
lo registrado y que el Panel refleje lo encontrado.

### 4.2 Matriz de búsquedas — 24 combinaciones

Cuatro ciudades × seis nichos. Una fila por ejecución.

| Ciudad | Restaurantes | Cafeterías | Dentistas | Veterinarias | Peluquerías | Hoteles |
|--------|:---:|:---:|:---:|:---:|:---:|:---:|
| Bogotá | □ | □ | □ | □ | □ | □ |
| Medellín | □ | □ | □ | □ | □ | □ |
| Cali | □ | □ | □ | □ | □ | □ |
| Barranquilla | □ | □ | □ | □ | □ | □ |

**Por cada ejecución se anota:** encontrados · descartados · motivo · devueltos ·
tiempo Places · tiempo análisis · tiempo total · incidencias.

---

## 5. ✅ Resuelto — WhatsApp fuera de alcance

**Registro histórico.** Los sprints H-12, H-12.1 y H-12.2 incluyeron WhatsApp en
la cadena a validar. La preparación de H-12.2 detectó que **el canal no está
construido**: el generador ofrece `Email frío`, `LinkedIn` e `Instagram`, y no
existe ningún enlace `wa.me` ni código que abra la aplicación. El backend sí lo
contemplaba parcialmente (`fallbackPitch.ts` tiene una rama `whatsapp`), pero le
faltaba toda la mitad de arriba.

**Decisión del PO en H-12.3: opción (a).** WhatsApp queda explícitamente fuera
del alcance de la hackathon. No es requisito de validación y su ausencia no
impide cerrar H-12.

El trabajo futuro está especificado en `docs/backlog/B-01 — WhatsApp Channel.md`,
como trabajo posterior a la hackathon.

---

## 6. 🟠 Observación — la clave viaja en la URL

El adapter envía la credencial **dos veces**:

```ts
const url = `https://places.googleapis.com/v1/places:searchText?key=${apiKey}`;
// ...
"X-Goog-Api-Key": apiKey,
```

La cabecera es suficiente. El parámetro de consulta es redundante y las claves en
URL son la vía habitual por la que acaban en logs de intermediarios.

**Riesgo real hoy: bajo.** Verifiqué que el mensaje de error se construye desde
`errData.error.message` o el código de estado — **la URL nunca entra en el log**,
así que la clave no se filtra por ahí. Es tráfico HTTPS servidor→Google.

**No lo cambio:** este sprint dice «no modificar nada si funciona». Lo dejo
anotado como higiene pendiente.

---

## 7. Validación visual con datos reales

Solo observar y anotar en H-13. **No corregir nada.**

### Datos del negocio
```
□ Nombres largos — ¿desbordan la tarjeta o el encabezado?
□ Nombres muy cortos — ¿queda hueco raro?
□ Categorías largas — ¿rompen la línea de badges?
□ Direcciones largas — ¿desbordan?
□ Sin teléfono — ¿se declara la ausencia o desaparece en silencio? (R-38)
□ Sin sitio web — ¿clasificación correcta?
□ Con varias webs / redes sociales — ¿cuál elige?
□ Sin reseñas — ¿el Score se comporta? (R-38: ausencia ≠ cero)
□ Rating extremo (5.0 con 2 reseñas · 2.1 con 800) — ¿tiene sentido el Score?
□ Muchísimas reseñas — ¿desborda el número?
□ Negocios cerrados permanentemente — ¿aparecen?
□ Duplicados visibles pese a la deduplicación
□ Imágenes ausentes — ¿hueco o marcador?
```

### Score
```
□ Score alto (>80) — ¿el desglose lo justifica?
□ Score bajo (<30) — ¿el desglose lo justifica?
□ Score ausente — ¿se declara? (R-45: typeof === "number")
□ La suma de categorías cuadra con el total
```

### Interfaz bajo datos reales
```
□ Textos truncados con «…» donde no debería
□ Overflow horizontal en cualquier ancho
□ Tarjetas de altura desbalanceada en la rejilla
□ Botones desalineados
□ Badges que se salen de su contenedor
□ Contraste legible en todos los estados
```

### Mensaje generado
```
□ ¿Afirma algo que no está en la lista de problemas? (integridad H-09)
□ ¿Inventa cifras, porcentajes o plazos?
□ ¿Promete bocetos o entregables inexistentes?
□ ¿El prompt se acerca a algún límite de longitud?
□ ¿El respaldo determinista se distingue del texto del modelo?
```

---

## 8. Protocolo de medición

Los tiempos salen del reporte de ejecución impreso en consola, **no de
estimaciones**. Por cada búsqueda se copia el bloque completo.

Promedios a calcular al final, sobre ejecuciones reales:

```
□ Tiempo medio de búsqueda (Places)
□ Tiempo medio de análisis (incluye scoring — ver §2)
□ Tiempo medio de generación del mensaje
□ Total de búsquedas ejecutadas
□ Total de Leads encontrados
□ Tasa media de descarte y su motivo dominante
```

---

## 9. Criterio de finalización de H-12.2

```
✅ Cadena verificada de extremo a extremo
✅ Instrumentación comprobada — un hueco documentado (tiempo de Score)
✅ Checklist de validación creado (este documento)
✅ Plantilla de H-13 creada
✅ Alcance cerrado: WhatsApp fuera de la hackathon (H-12.3) → B-01
⏸️ A la espera de las claves
```

**No queda ninguna tarea pendiente antes de pegar las API Keys.**
